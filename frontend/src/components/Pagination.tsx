import { ChevronLeft, ChevronRight } from 'lucide-react'; import { Button } from '@/components/ui'; import type { Page } from '@/types';
export function Pagination({ page, onPageChange }: { page: Page<unknown>; onPageChange: (page: number) => void }) { if (page.totalPages <= 1) return null; return <div className="flex items-center justify-between border-t border-slate-200 px-5 py-3"><p className="text-sm text-slate-500">Page {page.number + 1} of {page.totalPages} · {page.totalElements} total</p><div className="flex gap-2"><Button variant="secondary" className="min-h-8 px-2" disabled={page.first} onClick={() => onPageChange(page.number - 1)} aria-label="Previous page"><ChevronLeft className="h-4 w-4" /></Button><Button variant="secondary" className="min-h-8 px-2" disabled={page.last} onClick={() => onPageChange(page.number + 1)} aria-label="Next page"><ChevronRight className="h-4 w-4" /></Button></div></div>; }
