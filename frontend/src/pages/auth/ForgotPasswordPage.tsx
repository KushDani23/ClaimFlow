import { useState } from 'react'; import { Link } from 'react-router-dom'; import { Button, Field, Input } from '@/components/ui'; import { AuthShell } from './LoginPage';
export function ForgotPasswordPage() { const [sent, setSent] = useState(false); return <AuthShell title="Reset your password" subtitle="We’ll help you regain access to your account">{sent ? <div className="rounded-lg bg-emerald-50 p-4 text-sm text-emerald-800">If an account exists for that email, reset instructions will be sent when this backend capability is enabled.</div> : <form className="space-y-5" onSubmit={(event) => { event.preventDefault(); setSent(true); }}><Field label="Email address"><Input required type="email" autoComplete="email" placeholder="you@company.com" /></Field><Button type="submit" className="w-full">Send reset instructions</Button></form>}<Link className="mt-6 block text-center text-sm font-semibold text-brand" to="/login">Back to sign in</Link></AuthShell>; }
